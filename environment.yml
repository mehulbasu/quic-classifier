name: 740-project

channels:
  - rapidsai
  - nvidia
  - conda-forge
  - defaults

dependencies:
  - cudf=25.10
  - cuml=25.10
  - cuda-cudart
  - cuda-version=12
  - xgboost
  - dask
  - numpy
  - pandas
  - scikit-learn
  - pyarrow
  - joblib

prefix: /mydata/conda-envs/740-project
# conda env create -f environment.yml -p /mydata/conda-envs/740-project
# conda activate /mydata/conda-envs/740-project
# conda env update -f environment.yml -p /mydata/conda-envs/740-project --prune